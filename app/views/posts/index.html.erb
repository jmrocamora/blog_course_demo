		<h1 id="page_title">Cosas que h&eacute;	 escrito</h1>

		<div id="post_wrapper" class ="skinny_wrapper">
			<% @posts.each do |post| %>
				<div class="post">
					<p class="date"><%= post.created_at.strftime('%A, %e %B %Y')%></p>
					<h2><a href="#"><%= link_to post.title, post %></a></h2>
					<hr>
				</div>
			<% end %>
		</div>

		<div class="pagination">
			<span class="previous_page disabled">Previous</span>
			<em class="currrent">1</em>
			<a rel="next" href="#">2</a>
			<a class="next_page" href="#" >Next</a>
		</div>